{
  "name": "sheljustdoes.github.io",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "next dev -p 3500",
    "build": "next build",
    "postbuild": "node scripts/check-projects.mjs && node scripts/resume-sync.test.mjs",
    "start": "next start",
    "lint": "next lint",
    "preview": "next build && npx --yes http-server out -p 3502 -s -c-1 -o",
    "check": "next build && npx --yes tsc --noEmit",
    "test:ui": "python scripts/ui-smoke.py"
  },
  "dependencies": {
    "next": "^15.0.0",
    "react": "^18.3.0",
    "react-dom": "^18.3.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^20.14.0",
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0"
  }
}
